<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:gravity="start">

    <!-- Agent message container with background -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginEnd="32dp"
        android:background="@drawable/agent_message_background"
        android:padding="16dp">

        <TextView
            android:id="@+id/textViewMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp"
            tools:text="I can help you with your pizza order. What size would you like?"
            android:importantForAccessibility="yes" />

        <!-- Agent name indicator -->
        <TextView
            android:id="@+id/textViewAgentName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:textStyle="italic"
            android:layout_marginTop="4dp"
            android:layout_gravity="end"
            android:visibility="gone"
            tools:text="👨‍🍳 Chef Agent"
            tools:visibility="visible"
            android:importantForAccessibility="yes" />

    </LinearLayout>

</LinearLayout>